node_modules
.output
.wxt
dist
*.log
.DS_Store

# Chrome end-to-end test profile directory (left behind by some
# automated test runners; never part of the source tree).
.freebuff/

# Marketing assets generated by scripts/generate-store-assets.mjs.
# The generator script is checked in; only the rendered PNGs are ignored.
store/*.png
